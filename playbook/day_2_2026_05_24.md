# Day 2: May 24, 2026

## 💬 What We Discussed Today

We completed three major roadmap milestones today:
1. **Agent / Daemon Mode**: Exposing `--daemon` loops and signal handlers (`SIGINT`/`SIGTERM`) to let the analyzer act as an in-cluster Kubernetes agent.
2. **GitOps Auto-PR Prototype**: Automating Git branch creation, yaml resource resizing (using safety buffers: 25% CPU, 20% Memory), and Pull Request creation on GitHub.
3. **Prometheus Exporter & Grafana Integration**: Developing an embedded metrics server and ready-to-import Grafana dashboard to view sizing recommendations.

---

## 🎯 The Plan We Decided to Proceed With

We successfully implemented and verified all the above:
* **Embedded Metrics Server**: Built `exporter.py` using Python's standard `http.server` library to serve metrics `/metrics` on port `8000` with zero third-party dependencies.
* **Telemetry & Savings Metrics**: Exposed gauges for recommended sizing, current sizing, estimated CPU/Memory savings (current - recommended), severity counts, run counts, and license validity status.
* **Grafana Dashboard**: Created `grafana/dashboard.json` with dark-themed visual panels for total cores/bytes salvageable, active recommendations, scan run history, and a detailed optimization table.
* **Helm Integration**: Packaged `templates/grafana-dashboard.yaml` inside the Helm chart to generate a sidecar-auto-importable ConfigMap when `grafana.dashboard.enabled` is true. Exposed service and port settings in `values.yaml`.
* **Testing**: Added unit tests in `tests/test_exporter.py` validating metrics serializations and test HTTP requests, hitting 148 passed tests and 81.62% code coverage.
* **Validation**: Ruff linting and mypy typing check out with 100% clean passes.

---

## ⚠️ Key Obstacles & Risks Identified

1. **Event Mocking Timing**: In `test_cli.py`, global mock patching on `threading.Event.is_set()` got consumed by standard library synchronization inside `threading.Thread.start()` when starting the background exporter thread.
   * *Solution*: Isolated CLI tests by creating an autouse fixture in `tests/test_cli.py` to mock `start_exporter` and `registry` calls during CLI test execution, preventing thread initialization and port binding conflicts.
2. **Missing Test Fixtures**: Resolved pytest `requests_mock` issues by adding the dependency to `pyproject.toml` and updating the venv.
3. **Mypy Strict Typing**: Fixed type mismatches on dictionary keyword unpacking inside alerting tests and JSON properties in gitops response values.

---

## 🛠️ Next Technical Implementation Steps

1. **Review and Transition to Production YAML Parsing**: Upgrade manifest updates from standard `PyYAML` to `ruamel.yaml` to fully preserve document formatting, spacing, and comments in production repositories.
2. **Expand GitOps Support**: Implement authentication and commit logic for other platforms (e.g. GitLab, Bitbucket) and Enterprise OAuth methods.
